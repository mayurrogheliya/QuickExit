import 'package:flutter/material.dart';

class VisitorsTab extends StatelessWidget {
  const VisitorsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(24),
      child: Center(
        child: Text("Visitors Gate Pass Section"),
      ),
    );
  }
}
